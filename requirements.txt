streamlit==1.35.0
fastapi==0.111.0
uvicorn[standard]==0.29.0
python-dotenv==1.0.1
python-multipart==0.0.9

torch==2.3.0
transformers==4.41.2
anthropic==0.28.0
openai==1.30.0

pylint==3.2.2

# ESLint (for JavaScript linting) must be installed separately via npm:
#   npm install -g eslint
# This is handled automatically in the Docker images (see Dockerfile_fastapi / Dockerfile_streamlit).
